import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    gridContainer: {
        backgroundColor: 'white',
        alignItems: 'center',
        padding: 20,
        margin: 20
    },
})